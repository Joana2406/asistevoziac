import React from 'react';
import { View, Text } from 'react-native';

const Home: React.FC = () => {
    return (
        <View>
            <Text>Bienvenido a la aplicación IAC voice</Text>
        </View>
    );
};

export default Home;
